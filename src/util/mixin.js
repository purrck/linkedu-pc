import {getCurrentPageInfo, setCurrentPageInfo} from '@/util/util'

export default {
  methods: {
    getCurrentPage (route, params) {
      //getCurrentPageInfo(route, params)
    },
    setCurrentPage (route, page) {
      //setCurrentPageInfo(route, page)
    },

  }
}
